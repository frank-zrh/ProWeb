<Window x:Class="ProWeb.Client.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置 / 连接" Height="300" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="代理服务地址" Margin="0,6,12,6" VerticalAlignment="Center" />
        <TextBox x:Name="ServerUrlBox" Grid.Row="0" Grid.Column="1" Margin="0,6"
                 AutomationProperties.Name="代理服务地址"
                 TextChanged="ServerUrlBox_TextChanged" />

        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="ServerUrlError" Foreground="#c0392b"
                   FontSize="11" Visibility="Collapsed" Margin="0,0,0,4" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="设备标识 (Device ID)" Margin="0,6,12,6" VerticalAlignment="Center" />
        <TextBox x:Name="DeviceIdBox" Grid.Row="2" Grid.Column="1" Margin="0,6"
                 AutomationProperties.Name="设备标识" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="主页" Margin="0,6,12,6" VerticalAlignment="Center" />
        <TextBox x:Name="HomePageBox" Grid.Row="3" Grid.Column="1" Margin="0,6"
                 AutomationProperties.Name="主页" />

        <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="SaveButton" Content="保存" Width="88" Margin="0,0,8,0"
                    IsDefault="True" AutomationProperties.Name="保存设置" Click="SaveButton_Click" />
            <Button Content="取消" Width="88" IsCancel="True" AutomationProperties.Name="取消" />
        </StackPanel>
    </Grid>
</Window>
